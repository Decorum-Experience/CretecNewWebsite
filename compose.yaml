services:
  web:
    image: ghcr.io/decorum-experience/cretecnewwebsite:latest
    pull_policy: always
    restart: unless-stopped
    ports:
      - "80"
    depends_on:
      - api

  api:
    image: ghcr.io/decorum-experience/cretec-api:latest
    pull_policy: always
    restart: unless-stopped
    environment:
      - RESEND_API_KEY=${RESEND_API_KEY}
      - CONTACT_FROM=${CONTACT_FROM:-CRETEC Kontaktformular <kontakt@mail.cretec-schiffstechnik.de>}
      - CONTACT_EMAIL=${CONTACT_EMAIL:-service@cretec-schiffstechnik.de}
